@using Sandbox;
@using Sandbox.UI;
@using System;
@namespace Reprint
@inherits FactoryPanel

<root>
	<FactoryStepPanel Fact=@(this)>
		<Name>Diagonal Line</Name>
		<Config>
			<FactoryStrokeSelection @ShowConfigurator=@(workspace.UseConfigurator)
				@ShowBurnSponge=@(workspace.UseBurnSponge) @StrokeStep=@Fact></FactoryStrokeSelection>
		</Config>
	</FactoryStepPanel>
</root>

@code
{
	private FactoryDiag Fact { get => factory as FactoryDiag; }
	protected override FactoryStep CreateFactory() => new FactoryDiag();
}
